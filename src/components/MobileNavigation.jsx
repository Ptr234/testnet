import React from 'react'

const MobileNavigation = () => {
  // This component is now handled by the Header component
  // Keeping it as a placeholder for future mobile-specific navigation features
  return null
}

export default MobileNavigation